version: '3'
services:
  nginx: 
    image: nginx:latest
    container_name: production_nginx
    restart: always
    volumes:
      -  C:\Users\Trung\Desktop\data\myhtml/:/myhtml/
      - ./conf.d:/etc/nginx/conf.d
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - 80:80
      - 443:443
#   network_mode: "host"

 
