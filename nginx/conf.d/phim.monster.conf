server {
        listen 80;
        listen [::]:80;
        server_name phim.monster *.phim.monster;
        return 301 https://$host$request_uri;
}

server {
  listen 443 ssl;
  server_name *.phim.monster;
  ssl_certificate /myhtml/archive/phim.monster/fullchain1.pem;
  ssl_certificate_key /myhtml/archive/phim.monster/privkey1.pem;

  location / {
       set $h $host;
       if ($h ~ ([0-9]*).phim.monster) {
        set $port $1;
      }
      #set $port $h;
      #set $mot 10001.phim.monster;
      #if ($mot ~ ([0-9]*).phim.monster){
      #  set $port $1;
      #}
      proxy_pass  http://192.168.1.9:$port; 
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }
}

server {
  listen 443 ssl;
  server_name phim.monster;
  ssl_certificate /myhtml/archive/phim.monster/fullchain1.pem;
  ssl_certificate_key /myhtml/archive/phim.monster/privkey1.pem;
  location / {
        root /myhtml/overlead.co;
  index index.html index.htm index.nginx-debian.html;
  }
}
